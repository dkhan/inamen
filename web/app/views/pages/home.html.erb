<div class="hero">
  <p class="eyebrow">Text pattern research lab</p>
  <h1>Inamen</h1>
  <p class="lead">
    Discover and verify mathematical patterns in plain text.
    KJV is the flagship corpus; the engine supports multiple editions today.
  </p>
</div>

<section class="panel">
  <h2>Engine</h2>
  <dl class="stats">
    <div>
      <dt>Version</dt>
      <dd><code><%= @engine_version %></code></dd>
    </div>
    <div>
      <dt>Catalog features</dt>
      <dd><%= @feature_count %></dd>
    </div>
    <div>
      <dt>Indexer revision</dt>
      <dd><code><%= Rails.application.config.inamen.indexer_revision %></code></dd>
    </div>
  </dl>
</section>

<section class="panel">
  <h2>Bundled corpora</h2>
  <table>
    <thead>
      <tr>
        <th>Edition</th>
        <th>File</th>
        <th>Size</th>
        <th>SHA-256 (prefix)</th>
      </tr>
    </thead>
    <tbody>
      <% @editions.each do |edition| %>
        <tr>
          <td><code><%= edition[:id] %></code></td>
          <td><%= edition[:filename] %></td>
          <td><%= number_to_human_size(edition[:bytes]) %></td>
          <td><code><%= edition[:sha256] %></code></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</section>

<section class="panel">
  <h2>Explore</h2>
  <p>
    Browse the <%= link_to "feature catalog", features_path, class: "inline-link" %>
    — <%= @feature_count %> verified patterns with definitions and live counts.
  </p>
</section>

<section class="panel muted">
  <h2>Coming next</h2>
  <p>Discovery scans — find tokens and chapters with counts divisible by any integer.</p>
</section>
